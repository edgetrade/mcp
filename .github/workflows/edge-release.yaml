name: Release Edge MCP tooling

on:
  push:
    branches:
      - main

permissions:
  id-token: write
  contents: write

jobs:
  build-edge:
    name: Build Edge MCP binary
    strategy:
      matrix:
        target:
          - x86_64-unknown-linux-musl
          - aarch64-unknown-linux-musl
          - x86_64-pc-windows-gnu
          - x86_64-apple-darwin
          - aarch64-apple-darwin
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 1
          fetch-tags: true

      - name: Setup Rust
        uses: actions-rust-lang/setup-rust-toolchain@v1
        with:
          cache: true
          cache-shared-key: ${{ matrix.target }}
      
      - name: Find Tag
        id: tagger
        run: |
          echo "tag=$(git describe --abbrev=0 --tags)" >> "$GITHUB_OUTPUT"

      - name: Build Release
        uses: zijiren233/cargo-cross@v1
        with:
          command: build
          targets: ${{ matrix.target }}

      - name: Prepare binary for upload
        run: |
          if [ -f target/${{ matrix.target }}/release/edge ]; then
            rsync --archive --mkpath target/${{ matrix.target }}/release/edge dist/edge-${{ steps.tagger.outputs.tag }}-${{ matrix.target }}
          elif [ -f target/${{ matrix.target }}/release/edge.exe ]; then
            rsync --archive --mkpath target/${{ matrix.target }}/release/edge.exe dist/edge-${{ steps.tagger.outputs.tag }}-${{ matrix.target }}.exe
          fi
      
      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.target }}
          path: dist/*

  publish-edge:
    name: Publish to GitHub
    needs: build-edge
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 1
          fetch-tags: true
      
      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts
          merge-multiple: true
      
      - name: Prepare release binaries
        run: |
          rsync --archive --verbose --mkpath \
            --include='edge-*' \
            --exclude='*' \
            artifacts/ release/

      - name: Find Tag
        id: tagger
        run: |
          echo "tag=$(git describe --abbrev=0 --tags)" >> "$GITHUB_OUTPUT"

      - name: Create GitHub Release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh release create "${{steps.tagger.outputs.tag}}" \
            --title "Edge MCP Client ${{steps.tagger.outputs.tag}}" \
            --generate-notes \
            release/*

  publish-npm:
    name: Publish to NPM
    needs: publish-edge
    runs-on: ubuntu-latest

    permissions:
      id-token: write
      contents: read

    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 1
          fetch-tags: true
          
      - name: Make Node Happy
        run: corepack enable
      
      - uses: actions/setup-node@v6
        with:
          node-version: 24
          registry-url: 'https://registry.npmjs.org'

      - name: Find Tag
        id: tagger
        run: |
          echo "tag=$(git describe --abbrev=0 --tags | sed 's/^v//')" >> "$GITHUB_OUTPUT"

      - name: Publish to npm
        run: | 
          # See https://github.com/actions/setup-node/issues/1440
          unset NODE_AUTH_TOKEN
          npm version "${{steps.tagger.outputs.tag}}" --no-git-tag-version
          npm publish --access=public

  publish-openclaw:
    name: Publish to ClawHub
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 1
          fetch-tags: true
          
      - name: Make Node Happy
        run: corepack enable
      
      - uses: actions/setup-node@v6
        with:
          node-version: 24
  
      - name: Find Tag
        id: tagger
        run: |
          echo "tag=$(git describe --abbrev=0 --tags | sed 's/^v//')" >> "$GITHUB_OUTPUT"

      - name: Publish to ClawHub
        run: |
          npx clawhub@latest login --token ${{ secrets.CLAWHUB_TOKEN }} --no-browser
          npx clawhub@latest publish plugins/openclaw --slug=edge --name=Edge.Trade --version=${{steps.tagger.outputs.tag}}
        env:
          CLAWHUB_TOKEN: ${{ secrets.CLAWHUB_TOKEN }}

  publish-claude:
    name: Publish to Claude
    runs-on: ubuntu-latest

    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 1
          
      - name: Create marketplace.json
        run: |
          cat > marketplace.json << 'EOF'
          {
            "plugins": [
              {
                "name": "edge",
                "source": "./plugins/claude",
                "version": "${{ github.ref_name }}"
              }
            ]
          }
          EOF
      
      - name: Commit marketplace.json
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add marketplace.json
          git commit -m "chore: update marketplace for ${{ github.ref_name }}" || echo "No marketplace changes"
          git push origin main || true
